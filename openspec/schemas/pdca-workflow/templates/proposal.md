## Why

<!-- 解释这项变更的动机。它解决了什么问题？为什么是现在？ -->

## What Changes

<!-- 描述将要变更的内容。对新增能力、修改或删除要具体说明。 -->

## Capabilities

### New Capabilities
<!-- 将要引入的能力。将 <name> 替换为短横线命名标识符（例如 user-auth、data-export、api-rate-limiting）。每一项都会创建 specs/<name>/spec.md -->
- `<name>`: <该能力涵盖内容的简要描述>

### Modified Capabilities
<!-- 其 REQUIREMENTS 正在变更的现有能力（不仅仅是实现）。
     仅当规范级别的行为发生变化时才列出。每一项都需要一个 delta spec 文件。
     使用 openspec/specs/ 中现有的规范名称。如果没有需求变更则留空。 -->
- `<existing-name>`: <什么需求正在变更>

## Impact

<!-- 受影响的代码、API、依赖、系统 -->